import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function DashboardActivityPage() {
  return (
    <PlaceholderPage
      titleKey="menu.dashboardActivity"
      iconKey="activity"
      emptyTitle="최근 활동"
      emptyDescription="아직 기록된 활동이 없습니다."
    />
  );
}
