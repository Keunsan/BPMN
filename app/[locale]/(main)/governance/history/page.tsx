import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function GovernanceHistoryPage() {
  return (
    <PlaceholderPage
      titleKey="menu.changeHistory"
      iconKey="history"
      emptyTitle="변경 이력"
      emptyDescription="변경 이력 조회·감사 추적 기능을 준비 중입니다."
    />
  );
}
