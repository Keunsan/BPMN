import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function AdminUsersPage() {
  return (
    <PlaceholderPage
      titleKey="menu.usersPermissions"
      iconKey="users"
      emptyTitle="사용자/권한"
      emptyDescription="사용자·권한 관리 기능을 준비 중입니다."
    />
  );
}
